apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: Cheese-Rat-Cheese-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    # Add additional annotations as needed for your specific ingress controller
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: Cheese-Rat-Cheese.example.com  # Replace with your actual domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: Cheese-Rat-Cheese
            port:
              number: 80
  # Uncomment and configure if you want TLS
  # tls:
  # - hosts:
  #   - Cheese-Rat-Cheese.example.com
  #   secretName: Cheese-Rat-Cheese-tls